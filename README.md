# QR-Code-Generator
QR code generator (HTML + Tailwind + vanilla JS) that creates SVG QR codes and lets users download them. It includes sensible options (size, error correction, margin, colors, filename) and basic validation. You get a preview, copy-to-clipboard, and download-as-SVG. You're welcome.
